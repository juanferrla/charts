apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ tempalte "myapp.fullname" . }}
  labels:
    app: {{ template "myapp.name" . }}
    environment: {{ .Values.environment | default "dev" }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "myapp.name" . }}
  template:
    metadata:
      labels:
        app: {{ template "myapp.name" . }}
        environment: {{ .Values.environment | default "dev" }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
